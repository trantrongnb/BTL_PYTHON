# 🎭 Dự Án Nhận Diện Cảm Xúc Khuôn Mặt (Face Emotion Recognition)

Dự án này sử dụng mô hình học máy để nhận diện và phân loại **5 loại cảm xúc cơ bản** của khuôn mặt theo thời gian thực:

- **Bình thường (Neutral)**
- **Hạnh phúc (Happy)**
- **Tức giận (Angry)**
- **Buồn (Sad)**
- **Ngạc nhiên (Surprise)**

---

## 🚀 Các Bước Triển Khai Dự Án

### 1. Tải Source Code (Clone Repository)

Sử dụng lệnh `git clone` để tải toàn bộ mã nguồn của dự án về máy của bạn.

```bash
git clone git@github.com:trantrongnb/BTL_PYTHON.git
```

### 2. Cài đặt các thư viện

Chạy câu lệnh

```bash
pip install -r requirements.txt
```

### 3. Đứng trong thư mục PYTHON chạy câu lệnh và bạn có thể test trên locallhost

```bash
uvicorn backend.app:app --reload

```
